// Firebase is disabled in Expo Go. This stub keeps imports from crashing.
// When building a standalone app, swap in @react-native-firebase/app.
export const firebaseAuth = null;
export const db = null;
